# Place your raw onboarding call recordings here.
# Accepted formats: .mp3, .wav, .m4a, .ogg, .flac
# You may also place pre-transcribed .txt files here.
#
# File naming: any name is fine. Files are processed in sorted order.
# Pairs with demo_calls/ by index (first onboarding → first demo, etc.)
#
# These files are NOT committed to the repo (audio files are gitignored).
# Add your own recordings here when running the pipeline locally.
